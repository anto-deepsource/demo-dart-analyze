name: flutter_deepsource_demo
description: Dummy Flutter project for testing dart-analyze SARIF reporting to DeepSource.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
